using System.Windows.Forms;

// Designer stub kept so the WinForms template project remains valid.
// The real UI lives in Program.cs (StatusForm).
namespace Timekeeping.Agent.Tray;

public partial class Form1 : Form
{
    public Form1()
    {
        InitializeComponent();
    }
}
